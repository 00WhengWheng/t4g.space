{
  "name": "t4g.space",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@10.8.3",
  "workspaces": [
    "web",
    "mobile",
    "shared",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "web:dev": "turbo dev --filter=web",
    "mobile:dev": "turbo dev --filter=mobile",
    "shared:dev": "turbo dev --filter=shared"
  },
  "devDependencies": {
    "turbo": "^2.3.1"
  }
}
