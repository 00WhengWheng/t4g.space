{
  "name": "t4g.space",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@10.8.3",
  "scripts": {
    "build": "nx run-many -t build",
    "dev": "nx run-many -t dev --parallel",
    "lint": "nx run-many -t lint",
    "type-check": "nx run-many -t type-check",
    "web:dev": "nx run web:dev",
    "mobile:dev": "nx run mobile:dev",
    "shared:dev": "nx run shared:dev",
    "web:build": "nx run web:build",
    "mobile:build": "nx run mobile:build",
    "shared:build": "nx run shared:build",
    "types:build": "nx run types:build"
  },
  "devDependencies": {
    "@nx/eslint": "^21.5.2",
    "nx": "21.5.2"
  }
}
